---
description: User preferences and project conventions for pules-expo
---

## Package Manager
- **Always use `bun`** for package management instead of npm, yarn, or pnpm
- Examples: `bun install`, `bun add <package>`, `bun run <script>`
